import React from 'react'
import NewRecipeForm from '../components/newRecipe/newRecipeForm'

export default function NewRecipe() {
    return (
        <section>
            Note: Allow user to add a preExisting template or create a custom template. Also use unSplash for images search
            <NewRecipeForm/>
        </section>
    )
}
