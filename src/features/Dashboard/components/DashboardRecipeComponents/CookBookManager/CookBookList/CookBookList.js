import React from 'react';
import PropTypes from 'prop-types';
import styles from './CookBookList.module.css';

const CookBookList = () => (
  <div className={styles.CookBookList}>
    CookBookList Component
  </div>
);


export default CookBookList;
